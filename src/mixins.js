// const axios = require('axios');

const allMixins = {
    methods: {
        methodName(payload) {
            // perform some function
            // return data to be retturned
        }
    }
}
export default allMixins